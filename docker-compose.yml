version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./mudrex_data:/app/mudrex_data
    environment:
      - MUDREX_API_SERVER_HOST=0.0.0.0
      - MUDREX_API_SERVER_PORT=8000
